import React, { Component } from 'react';

class NotFound extends Component {
    render() {
      return (
        <div className="main container">Ooops, something went wrong.</div>
      );
    }
  }

export default NotFound;